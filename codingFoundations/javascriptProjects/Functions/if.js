let isOn = true; // variable need to match or NO command willshow 

if (isOn == true) {
    console.log('The light is on!')
};

//// top code is longer ^  - bottom code is shorter both codes do the same command.

if(isOn) {
    console.log('Wow the light is one!');
}

console.log(isOn == true); //// =assignment, == comparison,  === comarison
console.isOn(isOn);

let weather = 65;

if(weather <70) {
    console.log('Weather a jcacket')
};

console.log(weather < 70);

// In a conditinal, whatever is in the parenthesis will evaluate to TRUE. 